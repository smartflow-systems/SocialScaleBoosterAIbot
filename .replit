entrypoint = "server.js"
run = "npm run dev"
modules = ["nodejs-20"]

[env]
PORT = "5000"
NODE_ENV = "development"

[git]
# We sign on Ubuntu, so keep Replit auto-commits off.
autoCommit = false

[agent]
expertMode = true
