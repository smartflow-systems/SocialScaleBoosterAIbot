# SmartFlow AI - Node.js Deployment Configuration
# ================================================
# This file confirms the project's Node.js deployment setup

PROJECT_TYPE=nodejs
FRAMEWORK=express
FRONTEND=react-vite
BACKEND_RUNTIME=node
BUILD_TOOL=vite-esbuild

# Build Configuration
BUILD_COMMAND="./deploy-fix.sh"
START_COMMAND="npm start"
DEVELOPMENT_COMMAND="npm run dev"

# Environment Variables
NODE_ENV=production
PORT=3000

# Build Outputs
BACKEND_BUNDLE="dist/index.js"
FRONTEND_ASSETS="dist/public/"

# Dependencies
PACKAGE_MANAGER=npm
NODE_VERSION=20

# Deployment Target
PLATFORM=replit-autoscale
DEPLOYMENT_TYPE=nodejs

# Confirmation
PYTHON_VIRTUAL_ENV_REQUIRED=false
PYTHON_DEPENDENCIES_REQUIRED=false
PYTHON_BUILD_PROCESS=false

# Note: Any Python files in the project are for optional dual-mode functionality
# but the primary deployment should always use Node.js configuration.