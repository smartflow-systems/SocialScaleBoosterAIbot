{
  "name": "smartflow-ai",
  "version": "1.0.0",
  "type": "module",
  "description": "SmartFlow AI - Premium e-commerce social media bot platform",
  "main": "dist/index.js",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "start": "NODE_ENV=production node dist/index.js",
    "build": "npm ci && npm run check && vite build && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
    "deploy": "./deploy.sh",
    "health": "curl -f http://localhost:$PORT/api/health || exit 1"
  },
  "keywords": [
    "ai",
    "social-media",
    "ecommerce",
    "automation",
    "bot",
    "typescript",
    "react",
    "express"
  ],
  "license": "MIT",
  "author": "SmartFlow AI Team"
}